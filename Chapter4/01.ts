This video discusses the concept of interfaces.
Typescript provides interfaces which can be used to define custom types.

Here is an example:
interface Demo {
    name: string;
    exists?: boolean;
}
