This video discusses specifying javascript types.
It basically means we can explicitly specify the types of variables, return types of functions, etc in typescript.
Example:
function abc(a: string, b: string): string {
    return a+b;
}
